name: CI/CD
on:
  push:
    branches: [main, develop]
jobs:
  test:
    runs-on: ubuntu-22.04
    name: Testing
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.13.0
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Test
        run: npm test
  deploy:
    needs: test
    runs-on: ubuntu-22.04
    env:
      NO_D1_WARNING: true
      API_CREDENTIALS: "API Token"
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    name: Deploying
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.13.0
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Install wrangler
        run: npm install wrangler -g
        if: github.ref == 'refs/heads/main'
      - name: Set production environment variables
        run: echo "${{ secrets.FIREBASE_PROJECT_ID }}" | wrangler secret put FIREBASE_PROJECT_ID --env prod
      - name: Deploy to production
        run: wrangler publish --env prod
        if: github.ref == 'refs/heads/develop'
      - name: Set develop environment variables
        run: echo "${{ secrets.FIREBASE_PROJECT_ID }}" | wrangler secret put FIREBASE_PROJECT_ID --env dev
      - name: Deploy to develop
        run: wrangler publish --env dev
