name: CI/CD
on:
  push:
    branches: [main, develop]
jobs:
  test:
    runs-on: ubuntu-22.04
    name: Testing
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18.13.0
          cache: npm
      - uses: ozaytsev86/create-env-file@v1
        with:
          INPUT_ENV_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          INPUT_ENV_FIREBASE_USER_EMAIL: ${{ secrets.FIREBASE_USER_EMAIL }}
          INPUT_ENV_FIREBASE_USER_PASSWORD: ${{ secrets.FIREBASE_USER_PASSWORD }}
          INPUT_ENV_FIREBASE_ADMIN_EMAIL: ${{ secrets.FIREBASE_ADMIN_EMAIL }}
          INPUT_ENV_FIREBASE_ADMIN_PASSWORD: ${{ secrets.FIREBASE_ADMIN_PASSWORD }}
      - run: npm install
      - run: npm test
  # deploy:
  #   needs: test
  #   runs-on: ubuntu-22.04
  #   name: Deploying
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: cloudflare/wrangler-action@2.0.0
  #       with:
  #         apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  #         command: publish --env ${{ vars.ENVIRONMENT }}
